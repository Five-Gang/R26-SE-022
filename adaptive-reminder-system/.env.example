# Database
POSTGRES_USER=ars
POSTGRES_PASSWORD=ars_dev_password
POSTGRES_DB=ars_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# App
APP_ENV=development
APP_SECRET_KEY=change-me-in-prod-use-openssl-rand-hex-32
APP_LOG_LEVEL=INFO

# CORS — comma-separated origins allowed to hit the API
APP_CORS_ORIGINS=http://localhost:5173,http://localhost:3000
